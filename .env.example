# =============================================================================
# Dezi Gateway Dev Starter - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/dezi_gateway?schema=public"

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Base URL of your application (used for redirects)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Session secret - MUST be at least 32 characters
# Generate with: openssl rand -base64 32
SESSION_SECRET="your-super-secret-session-key-at-least-32-chars"

# -----------------------------------------------------------------------------
# Dezi OIDC Configuration
# -----------------------------------------------------------------------------
# Dezi issuer URL (the OIDC provider)
# Example: https://auth.dezi.nl or your test environment URL
DEZI_ISSUER="https://auth.dezi.nl"

# Your registered client ID with Dezi
DEZI_CLIENT_ID="your-dezi-client-id"

# Client secret (optional, only for confidential clients)
# DEZI_CLIENT_SECRET="your-dezi-client-secret"

# Redirect URI - must match what's registered with Dezi
DEZI_REDIRECT_URI="http://localhost:3000/auth/dezi/callback"

# -----------------------------------------------------------------------------
# Dezi Private Key for JWE Decryption
# -----------------------------------------------------------------------------
# Your RSA private key in PEM format for decrypting JWE tokens from Dezi.
# The corresponding public key must be registered with Dezi.
#
# SECURITY NOTES:
# - In development, you can paste the PEM key here (with \n for newlines)
# - In production, use a secret manager (AWS Secrets Manager, HashiCorp Vault)
#   or HSM for key storage
# - NEVER commit real private keys to version control
#
# Example format (replace with your actual key):
DEZI_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvgIBADANBgkqhkiG9w0BAQEFAASCBKgwggSkAgEAAoIBAQC...\n-----END PRIVATE KEY-----"

# -----------------------------------------------------------------------------
# Development / Debug Settings
# -----------------------------------------------------------------------------
# Set to 'development' for verbose logging, 'production' for minimal logging
NODE_ENV="development"
